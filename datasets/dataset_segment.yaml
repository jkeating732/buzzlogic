path: /path/to/dataset
train: images/train
val: images/val

nc: 4

# Class names
names: ['Capped Honey', 'Drone Cells', 'Queen Cell', 'Worker Brood']

# Weights are IMPORTANT! If you change # of annotations or classes, calculate new weights by:
# (TOTAL ANNOTATIONS) / (NUM CLASSES * ANNOTATIONS FOR CLASS X)
# You can find total annotations and annotations for a class by running annotations.py
# Your project.json MUST be updated for this to properly work

weights: [1.22058, 2.3056, 0.83, 0.64843]